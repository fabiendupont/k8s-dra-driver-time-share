apiVersion: v1
kind: Pod
metadata:
  name: deadline-workload
spec:
  containers:
    - name: workload
      image: busybox:latest
      command: ["sleep", "infinity"]
      resources:
        claims:
          - name: time-slot
  resourceClaims:
    - name: time-slot
      resourceClaimName: my-time-slot
