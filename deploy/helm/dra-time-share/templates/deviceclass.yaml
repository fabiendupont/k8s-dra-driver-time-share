apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: {{ include "dra-time-share.fullname" . }}-slots
  labels:
    {{- include "dra-time-share.labels" . | nindent 4 }}
spec:
  selectors:
    - cel:
        expression: "device.driver == '{{ .Values.driverName }}'"
